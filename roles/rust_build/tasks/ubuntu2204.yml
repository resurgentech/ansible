---
- name: Ubuntu2204 tensorflow build Dependancies
  apt:
    name:
      - libstdc++-12-dev
    state: present

- name: Install rust
  shell: curl https://sh.rustup.rs -sSf | sh -s -- -y
  args:
    warn: false